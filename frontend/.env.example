# --- Monitor Frontend Template ---
# Public URL of the backend API (must start with http/https)
# If using Nginx subpath: http://your-domain.com/monitor-api
NEXT_PUBLIC_API_URL=http://localhost:8000
